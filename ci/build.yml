---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: 1.26.1-bookworm

inputs:
  - name: dis-legacy-cache-purger

outputs:
  - name: build

caches:
  - path: go/

run:
  path: dis-legacy-cache-purger/ci/scripts/build.sh
